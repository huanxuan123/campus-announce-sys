<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.announce.mapper.user.UserMapper">

    <resultMap id="BaseResultMap" type="com.campus.announce.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="user_type" property="userType"/>
        <result column="dept_id" property="deptId"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="student_no" property="studentNo"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE username = #{username}
    </select>

    <select id="selectByStudentNo" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE student_no = #{studentNo}
    </select>

    <select id="selectByDeptId" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE dept_id = #{deptId}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_user ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.campus.announce.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, user_type, dept_id, email, phone, student_no, status)
        VALUES (#{username}, #{password}, #{realName}, #{userType}, #{deptId}, #{email}, #{phone}, #{studentNo}, #{status})
    </insert>

    <update id="update" parameterType="com.campus.announce.entity.User">
        UPDATE sys_user
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>

    <update id="updatePassword">
        UPDATE sys_user SET password = #{newPassword} WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE sys_user SET status = #{status} WHERE id = #{id}
    </update>

</mapper>